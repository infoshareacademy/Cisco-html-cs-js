// Napisz definicję funkcji getUniqueEmails która zwróci tablicę unikatowych adresów email (bez powtórzeń)
// Wykorzystaj pętlę for of

function getUniqueEmails(emails) {
 // Twój kod
}

const emailList = [
  "jan@example.com",
  "anna@example.com",
  "jan@example.com",
  "kuba@example.com",
  "anna@example.com"
];

const unique = getUniqueEmails(emailList);

console.log(unique);